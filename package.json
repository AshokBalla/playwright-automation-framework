{
  "name": "playwright-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation framework with API helpers, fixtures, and browser matrix support.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/**/*.test.cjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
